# KAJSYS agent entry

Work only in `C:\Projects\123W-cell\KAJSYS` or in a task worktree created from it. The canonical remote is `https://github.com/123W-cell/KAJSYS` and the default branch is `main`.

Read `README.md`, fetch `origin`, and report branch, exact HEAD, clean/dirty state, and ahead/behind before editing. This is a static dashboard. Its public hosting target is currently not verified, so do not claim that a push deploys it and do not enable or change hosting without explicit release authority.
